/*
package com.example.iot_project_backserver.repository;

import com.example.iot_project_backserver.entity.EcgAverage;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import java.math.BigInteger;
import java.util.List;

@Repository
public interface EcgAverageRepository extends JpaRepository<EcgAverage, String> {
    // userid로 average_value 조회
    List<EcgAverage> findByUserid(String userid);
}
*/
